# ZAO OS Daily Brief — 2026-04-02

## Branch & Changes

- **Branch:** `main` (up to date with `origin/main`)
- **Uncommitted work:** None — clean working tree
- **Open PRs:** None (all recent PRs merged)

## Recent Activity

The last 10 commits show a productive sprint around public-facing pages and Farcaster miniapp integration. Key shipped work: ZAO Festivals page (`/festivals`), ZAO Stock landing page (`/stock`), fc:miniapp per-page embed tags with new API tests, sponsor tier copy fixes, and festival image organization. All landed via PRs #87–90 on the `feat/` branches.

## Open Work

- **PRs:** 0 open
- **Issues:** Unable to query (no `gh` CLI available in this environment)

*Note: Use `gh issue list --state open --limit 5` locally to check issues.*

## Health

- **Lint:** ❌ Broken — `ESLint: 10.0.0` throws `ERR_MODULE_NOT_FOUND` for `eslint` package imported from `eslint.config.mjs`. This is likely a missing `node_modules` or a version mismatch after a dependency change.
  - **Fix:** Run `npm install` to restore the `eslint` package, then re-run `npm run lint`.

## Today's Focus

1. **Fix lint** — `npm install` likely resolves the ESLint ERR_MODULE_NOT_FOUND. Confirm with `npm run lint` before any new code ships.
2. **Public page polish** — Festivals and Stock pages just landed. QA both on mobile, check SEO meta tags and fc:miniapp embeds are correct.
3. **Distribution pillar** — With `/festivals` and `/stock` live, next natural step is artist distribution tooling (DistroKid-style one-click publishing or split-payment setup for releases). See `research/029-artist-revenue-ip-rights/`.
4. **Governance catchup** — No governance activity in recent commits. Consider running a Snapshot weekly priority poll to re-engage the 188-member community.
5. **Research → Build** — See today's inspiration doc (`research/inspiration/2026-04-02.md`) for three concrete UX patterns to steal from Sound.xyz, Stem, and Are.na.
