# ZAO OS Daily Brief — 2026-04-02

---

## Branch & Changes

- **Branch:** HEAD detached from `refs/heads/main` (clean — `git checkout main` recommended)
- **Uncommitted work:** None. Working tree clean.

---

## Recent Activity

Last 10 commits show the merge of **PR #96** (`feat/zao-stock-improvements`) — additions to the `/stock` landing page including What is The ZAO, After-Party, Past Events, FAQ, social links, OG image, a tax-deductible notice (NMC/Fractured Atlas), and a `/zao-stock` Claude skill. Also includes a `/festivals` page, miniapp security best-practice fixes (PR #93, #94), and a TS build error fix (PR #92). The repo has been in active content + polish mode — no new infrastructure features in the last sprint.

---

## Open Work

- **PRs:** None open (no `gh` CLI available in this environment — check GitHub directly).
- **Issues:** None retrieved (same reason). Check `https://github.com/bettercallzaal/zaoos/issues`.

---

## Health

- **Lint:** FAILING — `ESLint: 10.0.0` cannot find package `eslint` imported from `eslint.config.mjs`. This is a missing `node_modules` issue — run `npm install` to restore. Not a code quality problem, likely a fresh env without dependencies installed.
- **Build:** Not run (would fail without lint passing first).

**Action required:** `npm install` before any build/deploy.

---

## Today's Focus

Based on recent activity and the gap analysis from yesterday's inspiration research:

1. **Fix environment:** `npm install` — lint is broken due to missing node_modules.
2. **Revenue pillar (highest-leverage, scored 2/10 yesterday):** The governance → revenue bridge is the single most important missing feature. Today is a good day to scaffold `src/app/api/revenue/splits/route.ts` and `src/lib/revenue/splitEngine.ts` if Zaal is in build mode.
3. **HEAD detached state:** Run `git checkout main` to reattach to the branch before making any commits.
4. **Content follow-up:** `/stock` and `/festivals` pages are now live — consider posting build-in-public notes about them (Farcaster + Bluesky + X via cross-publishing).
5. **Artist development (scored 4/10):** After revenue, consider a ZAO Boards feature (slow curation layer proposed yesterday in `research/inspiration/2026-04-01.md`).
