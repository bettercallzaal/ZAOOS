# Morning Briefing — May 6, 2026 (Wednesday)

GM Zaal. Wednesday, May 6.

---

## TODAY'S 3 PRIORITIES

1. **Deploy ZabalConviction to Base** — Day 25. `cast send` or `npx hardhat deploy --network base`. $0.50 gas. Contract address → update `.env.local` + `community.config.ts`. Everything downstream (agent wallets, BANKER burn loop, staking) is blocked by this single command. Day 25 is not acceptable. Today.
2. **Fix AgentMail** — Day 25. `AGENTMAIL_API_KEY` in `.env.local` + `api.agentmail.to` in `.claude/settings.json` `allowedHosts`. ZOE has been inbox-blind for 25 nights. Five minutes. No context needed.
3. **PR #465 checklist** — The bridge is merged. Run the 4 pending items: RECALL test via Telegram, update `SOUL.md`, update `AGENTS.md`, disable Telegram notifications in the group. This activates the full autonomous operating space.

---

## OVERNIGHT

- **Inbox:** 0 items processed — `api.agentmail.to` blocked at host allowlist. Day 25.
- **Newsletter draft:** Ready at `docs/daily/2026-05-06-newsletter.md`
- **PRs merged today (May 5):** 7
  - PR #465 — doc 599: ZAO Bonfire Bridge as multi-agent operating group ✓ MERGED
  - PR #467 — doc 601: Agent stack cleanup — kill list + collapse to 3 surfaces
  - PR #469 — doc 603: TradingAgents pattern → ZAO social + PM
  - PR #471 — doc 605: Agentic tooling May 2026 survey
  - PR #473 — doc 607: Three bots, one substrate (operating model)
  - PR #475 — docs(events): May 4 meeting transcripts (608, 609)
  - PR #466 — docs: ZOE nightly processing 2026-05-04

---

## THOUGHT PROMPT

Yesterday was the architecture day. Doc 601 killed 7 systems and collapsed the stack to 5. Doc 607 defined the substrate model — Bonfire as the single source of truth, three trust tiers, ZOE as Zaal's dispatcher across all surfaces. The operating model is written, reviewed, and merged.

Here's the connection worth making: doc 601 found that openclaw was "the right idea with the wrong brain." The ZOE concierge concept was correct from day one. Only the Minimax implementation was wrong. The fix wasn't to redesign the concept — it was to plug the right runtime (Hermes/Claude Code subprocess) into the existing identity.

That same pattern is on the blockers list. ZabalConviction is the right contract with the wrong deployment status. AgentMail is the right inbox with the wrong env var. The concept isn't broken. The five-minute implementation fix is pending.

Doc 607 establishes that new facts default to the most-restrictive trust tier (`ZAAL_PRIVATE`). Promotion requires an explicit gesture. That's the model for today: default to the most important thing, require an explicit action to move forward. The most-restrictive priority is the Day 25 deploy. Everything else can wait for that promotion.

---

## CALENDAR

- No scheduled meetings today (Wednesday).
- ZAOstock post-graduation: monitoring `zaostack.com` for post-launch issues.
- Adrian V3 follow-up: Day 21. Escalate from one-liner to dual-channel (Discord + email same day). Document outcome either way.

---

## BLOCKERS DASHBOARD (Day 25)

| Item | Days | Action |
|------|------|--------|
| ZabalConviction deploy | 25 | `cast send` — Base mainnet. $0.50. One command. |
| AgentMail API + allowlist | 25 | `.env.local` key + `.claude/settings.json` `allowedHosts` |
| Fund agent wallets | 25 | $15 ETH × 3 wallets on Base (waits on ZabalConviction) |
| Privy wallet setup | 25 | Provision addresses, update `.env.local` |
| Adrian V3 API docs | 21 | Escalate: Discord + email, same day. Document result. |
| Vercel OAuth breach audit | 25 | P0. doc 471. Confirm completed or escalate. |

---

## ARCHITECTURE LOCKED (as of yesterday)

| Decision | Status |
|----------|--------|
| Agent stack: 3 daily surfaces + 2 background specialists | Locked (doc 601) |
| Kill list: openclaw, ZAO Devz, bot fleet plan, FISHBOWLZ | Executed (doc 601) |
| Bonfire = substrate (3 trust tiers) | Locked (doc 607) |
| ZOE = dispatcher (single Zaal relationship) | Locked (doc 607) |
| PR #465 checklist | **Pending — run today** |
| ZOE `bot/src/zoe/` Hermes-style scaffold | **SHIPPED** — deploy to VPS next |
