Lunch window — 3 things:

1. **Add AGENTMAIL_API_KEY to .env.local** — Day 25 and ZOE is still inbox-blind. 2 min from phone via Vercel env or SSH. Also add `api.agentmail.to` to `.claude/settings.json` allowedHosts.
2. **Follow @adrienne + @warpee.eth on Farcaster** — GMFC101 RAG stack + Word-A-Day pattern. Both map directly to ZAO. Quick tap, no context needed.
3. **Decision: ZabalConviction deploy today or slip to tomorrow?** — $0.50 gas, `cast send` or hardhat. Agent wallets, BANKER burn loop, and staking are all blocked behind this. Make the call before you sit back down.

Task status: 0/52 priorities done. Big list, one unblocking move changes everything — ZabalConviction deploy is the domino.
