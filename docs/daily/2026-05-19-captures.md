# Captures — May 19, 2026 (Tuesday)

---

## Inbox

**AgentMail blocked — Day 34.** `api.agentmail.to` not in network allowlist.
0 items processed tonight.

Status: Host allowlist fix remains the #1 carry blocker.

---

## Today's PR Activity (3 new PRs opened)

| PR | Title | Notes |
|----|-------|-------|
| #568 | `feat(bonfire-ingest): pipeline + secret-scan filter (sanitized)` | Replaces PR #567 which had a real Telegram bot token in a test fixture — caught by GitHub Secret Scanning. Token rotated via @BotFather (pending Zaal confirm). Bonfire ingest now gates ALL content through a 25+ regex pattern scanner. HIGH severity = block, MED/LOW = log+post. |
| #569 | `doc 674: Edge Esmeralda + Artizen ZAO Fund + Telamon outreach (STANDARD)` | Telamon Ardavanis runs a fund literally called "ZAO Fund." Edge Esmeralda May 30–June 27, Healdsburg CA. First action: Zaal sends X DM to @TelamonArdavani **today (May 19)**. Verify Season 6 grant pool details in browser before send (Next.js client-render, not in static HTML). |
| #570 | `feat(skill): /meeting — capture transcripts + distribute (doc 673)` | New `/meeting` Claude Code skill. Built same day the Telegram bot hit a 4096-char output limit. Six inputs, six output targets. Whisper transcription via Iman VPS. Cascade extraction: Haiku 4.5 first, Opus 4.7 on low-confidence. No bot API dependency. |

### Carried Open PRs (from May 18, still awaiting review)

| PR | Title | Blocking? |
|----|-------|-----------|
| #564 | doc 673: ZAO Craig spec (Telegram) | Superseded by #566. Merge to close, note superseded. |
| #565 | doc 673: ZOE ↔ Bonfires dialog (DISPATCH) | Numbering collision with #564. Needs renumber to 675 before merge. |
| #566 | doc 674: ZAOscribe Discord pivot (DEEP) | Supersedes #564. Key spec: per-speaker audio via native Discord streams, no diarization. 34hr build, 7 phases. |
| #546 | doc 664: Frapp-GH async fractal (FIP #19) | 7 questions for Tadas + Dan awaiting Zaal read + DM |
| #547 | doc 663: ecosystem meta-audit | 3 P0 actions awaiting Zaal approval |
| #549 | doc 665: ZABAL haatz swap | 70–90% Neynar cost reduction, low risk |

---

## Categorized Items

### SECURITY INCIDENT: PR #567 Telegram Token Leak
A real Telegram bot token was accidentally included in a self-test fixture in PR #567. GitHub Secret Scanning caught it immediately on push. PR #567 was closed + branch deleted from origin. Token rotated via @BotFather (pending Zaal confirm in BotFather chat). PR #568 replaces it with synthetic FAKE-marked fixtures and a `secret_scan.py` scanner with 25+ regex patterns. The scanner distinguishes template placeholders (`KEY=your_key_here`) from real high-entropy values. **Going forward: every Bonfire ingest must gate through `IngestPipeline`.**

### Research: Doc 674 — Edge Esmeralda / Artizen / Telamon
Telamon Ardavanis (Edge City co-founder) curates the Artizen "ZAO Fund for Emerging Culture" Season 6. Edge Esmeralda is May 30–June 27 in Healdsburg CA. The naming overlap is not coincidental — it's a hook. Zaal's single ask = 30-min 1:1. First contact method: X DM (@TelamonArdavani). Email not public.

### Feature: /meeting Skill
The Telegram bot's 4096-char output limit surfaced during a real coworking session. The `/meeting` skill is the workflow layer that bypasses the bot entirely — paste a transcript (or provide an audio file), get action items extracted and distributed. Locked decisions: Whisper on Iman VPS, confirm-each memory write, print copy-paste block for Telegram instead of API call.

### Architecture: ZAOscribe Discord Pivot
Discord's `@discordjs/voice` v0.19+ gives per-speaker audio streams natively (`connection.receiver.subscribe(userId)`). No pyannote, no speaker-embedding model, free diarization. PR #566 locks this as the production approach for ZAO cowork capture. CraigChat (MIT, 270+ years audio annually) is the reference, not the fork. Build is ~34 hours across 7 phases, each shipping a demoable commit.

### Ideas
- Bonfire ingest secret scanner is general-purpose infrastructure — any agent that writes content anywhere should run through this.
- The `/meeting` skill + ZAOscribe are solving the same problem from different angles (sync vs async). They should probably share the extraction schema.
- Edge Esmeralda is 11 days out. If Telamon replies, this could be in-person at the event.
- PR #565 (ZOE ↔ Bonfires) needs a renumber to 675, then it can merge. That unblocks Phase 2 of the ZOE-Bonfires integration (265 LoC across 3 files).

### Tasks Identified
- URGENT: Confirm Telegram bot token rotation via @BotFather
- Send X DM to @TelamonArdavani (Edge Esmeralda, ZAO Fund — first action from PR #569)
- Review + merge PR #568 (Bonfire ingest pipeline — security improvement)
- Review + merge PR #569 (Edge Esmeralda research)
- Review + merge PR #570 (/meeting skill)
- Renumber PR #565 doc from 673 → 675, then merge
- Review PR #566 (ZAOscribe Discord spec)
