# Morning Briefing — May 22, 2026 (Friday)

GM Zaal. Friday May 22.

---

## TODAY'S 3 PRIORITIES

1. **Merge PR #592 + PR #585** — Both are P0. The ZABAL vote-power bug is in production (every score defaulted to 0.5 since haatz integration). The pre-commit secret scan hook was inert (Telegram token + test key leaked this month). Merge both before anything else.
2. **Bonfire labeling** — Day 37. One admin action in the bonfire dashboard. Unlocks ZOE recall, /search in ZAOcoworkingBot, governance digests. This keeps carrying because it takes 2 minutes and keeps getting skipped.
3. **Weekly recap + 4:30pm deadline** — Friday. `docs/weekly/2026-W21-recap.md` + 5-post Farcaster thread draft.

---

## OVERNIGHT

- Processed 0 inbox items (AgentMail blocked — Day 36. `api.agentmail.to` still not in allowlist.)
- Newsletter draft ready at `docs/daily/2026-05-22-newsletter.md`
- 8 PRs merged today (PRs #572-580, skipping #577): Tanja recap, Bonfires utilization, Bonfire-GitHub connection, ZAOstock advisor recap, meeting-bonfire-bridge docs, ZAOstock May 19 initiatives, coworking pipeline
- Meeting skill iterated through 5 autoresearch versions (v0 → v2 with index, distribution routing, confident phase detection)
- **16 open PRs** — 2 code PRs (P0 and P0-security), 14 research docs ready to batch-merge

---

## KEY FINDINGS FROM YESTERDAY

**ZABAL vote-power bug (PR #592):** `haatz.quilibrium.com/v2/farcaster/user/bulk` returns HTTP 200 with `experimental: null`. The failover logic interprets 200 as success, never falls through to Neynar. Every vote score has been defaulting to 0.5 (half-multiplier for everyone) since the haatz integration shipped. Fix is in PR #592.

**ZOE archive amnesia (PR #582):** ZOE has a permanent on-disk archive but `readArchive()` is never called in the context load path. Every ZOE session is amnesiac past 8 turns. The fix plan is in the doc — just needs the wire.

**Pre-commit hook was inert (PR #585):** `.husky/pre-commit` existed but was not running. A Telegram token and a test-fixture key leaked this month because of this. PR #585 is the fix.

**2026-06-15 cost bomb:** `claude -p` headless usage moves from Max interactive limits to a separate Agent SDK credit (then raw API cost) on June 15. The cowork `/code` pipeline (4 users, Opus runs) needs the `HERMES_FLEET_DAILY_USD_CAP = 10` live before that date.

---

## THOUGHT PROMPT

Yesterday's bugs are connected by the same failure mode: things that look like they're working because they don't throw errors. The haatz 200-OK silenced the failover. The archive write silenced the read-miss. The hook file silenced the scan absence. The lab ran for weeks with all three gaps open, and none of them announced themselves.

The doc 694 re-research campaign (117 thin docs, wave 1 shipped yesterday) is the same pattern at the library level: docs that looked complete because they had content, but the content was written from metadata because the real fetch never ran.

The question for this week's recap: what else is passing silently? Where else are we building on assumptions that aren't checked?

Ryan Kagy called this out on the ZOE archive bug specifically — he's building a compiled ZOE that loads the lifestream. Worth following up on what he shipped (PR #582 has the manifest for him).

---

## CALENDAR

- **Weekly recap due at 4:30pm** — Friday tradition. `docs/weekly/2026-W21-recap.md`.
- No Fractal meeting today (Monday only).
- Eden Fractals bi-weekly Thursday — Tanja now joins (this was yesterday, doc 675 filed).
- Artizen ZAO Fund S6: ~32 days remaining. Grantee emails are time-sensitive.
- **2026-06-15**: Mark it. `claude -p` headless billing changes. Verify `HERMES_FLEET_DAILY_USD_CAP` before that date.

---

## OPEN PRs REQUIRING ACTION

| PR | Title | Action |
|----|-------|--------|
| **#592** | **ZABAL vote-power fix + /zabal/demo** | **MERGE — P0 bug in production** |
| **#585** | **feat(hooks): pre-commit secret scan** | **MERGE — P0 security, before next push** |
| #596 | Doc 695 Crypto Factor + Avalanche | Review + merge; reply to Matt with soulbound constraint |
| #595 | Doc 695 Juke integration | Review + merge |
| #594 | Doc 695 ZABAL Games context prompt | Zaal review: voice + mechanics; renumber (collision with #595/#596) |
| #593 | Re-research wave 1 (20 Farcaster docs) | Review + merge |
| #591 | /zao-research skill fix v2.3 | Review + merge |
| #590 | Bot fleet architecture (locked) | Review + merge |
| #589 | Docs 687-691 + /inbox cluster mode | Review + merge |
| #588 | DB consolidation (ZAOstock + ZAOcoworking) | Review + merge |
| #587 | Docs 684-686 | Review + merge |
| #586 | Artizen + ZAO Fund director guide | Review + merge (time-sensitive: grantee emails) |
| #585 | Pre-commit secret scan | **MERGE** |
| #584 | ZAO ecosystem consolidation audit | Review + merge |
| #583 | ZAOstock task tracking + six-circle | Review + merge |
| #582 | ZOE troubles + Ryan followup | Review + merge; then build the archive fix |
| #581 | Ryan Kagy Limitless call recap | Review + merge |
| #549 | Doc 665 ZABAL haatz swap | 70-90% Neynar cost savings — action this week |
| #547 | Doc 663 ecosystem meta-audit | 2 P0s: archive dead repos + collision hook |
| #546 | Doc 664 Frapp-GH async | DM Tadas + Dan with 7 questions |
