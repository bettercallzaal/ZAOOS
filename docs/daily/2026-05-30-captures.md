# ZOE Captures — May 30, 2026

*Processed: 2026-05-30 22:00 EST by ZOE Nightly Processor*

---

## Inbox Status

**AgentMail fetch: SKIPPED** — `AGENTMAIL_API_KEY` not available in this cloud session environment (no `.env.local` present in container). This is **Day 40+** of inbox blindness.
**Fix:** Add `api.agentmail.to` to `.claude/settings.json` allowlist. 5 minutes. Highest-ROI unblocked item on the board.

---

## Git Activity — Today (Saturday May 30)

### PRs Merged Today

| PR | Branch | What |
|----|--------|------|
| #732 | ws/zoe-orchestrator-audit-770 | docs(770): ZOE orchestrator code audit (post-merge, pre-trust) |
| #731 | ws/research-bonfires-kernel | docs(771): Bonfires procedural memory kernel FCG (STANDARD tier) |

### Commits Landed

| SHA | Message |
|-----|---------|
| `d23dba3` | Merge PR #732 — ZOE orchestrator audit |
| `6f68232` | Merge PR #731 — Bonfires kernel research |
| `dea61397` | docs: identity research doc 771 - Bonfires procedural memory kernel FCG |
| `13304b70` | docs(770): ZOE orchestrator code audit (post-merge, pre-trust) |
| `4ecef8b3` | fix(761/762): QKMS verdict (noble is THE signer) + green bot typecheck |
| `bac9c4d2` | Merge PR #729 — ZAO Farcaster multi-agent |
| Various | feat(761) Phases 1–4: Farcaster signer, caster, multi-agent layer, FFX exec wrapper |

---

## PRs Open Tonight (Awaiting Merge)

### PR #733 — ZOE Orchestrator HIGH Fixes
**Branch:** `ws/zoe-orchestrator-high-fixes-771`
**Status:** Ready to merge. 82 ZOE tests pass; tsc clean.
**What:** Fixes the 5 HIGH findings from the doc 770 audit:
- **H1** — Command prefixes (`plan:`/`note:`/nudge) no longer bypass pending-approval interception
- **H2** — No silent clobber: `handlePlanCommand` warns on unresolved approval, scheduler crons skip arming when user approval is live
- **H3** — Real cost/safety guard: `MAX_SUBTASKS` ceiling, per-wave budget pre-flight (stops before spawning), bounded concurrency replacing `Promise.allSettled(map)`
- **H4** — Hardened worker Bash denylist (`mv`/`dd`/`chmod`/`tee`/`sh`/`eval`/`node`/`npx`/…). Partial — denylist can't catch redirections; allowlist + `permissionMode` deny-by-default is the true guard. Needs live CLI verification on VPS after merge.
- **H5** — `runApprovedPlan` fully wrapped in try/catch
- **Bonus:** Unbreaks `bot` tsc — `subscribeToCasts` non-void promise fixed

### PR #734 — Doc 772: Supabase-Native CRM (supersedes Airtable)
**Branch:** `ws/research-crm-supabase-native`
**Status:** Ready to merge. Docs only — no schema applied (migration SQL is a PROPOSAL pending Zaal sign-off per CLAUDE.md).
**What:** Full research doc for migrating the ZAO relationship CRM off Airtable onto a Supabase-native, RLS-gated two-layer model.
- Key decision: private base table + public VIEW with `security_invoker=true` (NOT two tables, NOT column-level security)
- Per-row visibility enum on interactions
- `(select auth.uid()) + owner_id` index for RLS perf (179ms → 9ms benchmark)
- Bot writes via Bearer-auth `/api/crm/interactions`; browser via iron-session
- No Neo4j v1 — Postgres + Bonfire is the graph layer
- Reverses doc 737's Airtable-canonical decision per Zaal's 2026-05-29 call

### PR #736 — Doc 771 Trimtab Correction
**Branch:** `ws/fix-doc-771-trimtab`
**Status:** Ready to merge. Correction to the Bonfires kernel doc (just merged in PR #731).
**What:** 4-agent DEEP cross-ref refuted the Key Decision that "NERDDAO trimtab is the FCG kernel's likely home."
- **trimtab** is Tracery + n-gram + HDBSCAN + a real-ladybug vector store (~156KB Python, last updated 2026-04-22). NOT the FCG kernel.
- **FCG kernel** is proprietary/unreleased — zero hits for GLiNER/FCG/RoleBoundEventFrame/TemporalEdge/MentionCluster across ~50 NERDDAO repos.
- Only public GLiNER footprint: `NERDDAO/graphiti` (fork of `getzep/graphiti`, Apache 2.0) — likely the substrate Bonfires extends.

---

## Key Themes Today

### ZOE Orchestrator — Audit → Fix cycle complete (pending merge + VPS deploy)
Today was the trust-building session for ZOE's autonomous orchestrator. Doc 770 (audit, merged) + PR #733 (HIGH fixes, open) form a pair: you don't hand an agent cost-incurring autonomy until you've read its code and patched the holes. H3 (budget preflight) is the most important fix — it's the difference between ZOE stopping before a $10 research wave and stopping after.

The "82 tests pass, tsc clean" result is the bar. After PR #733 merges and VPS redeploys, the smoke test (`plan: test routing` should route, not ACK) is the final gate.

### Bonfires Kernel — What we know vs. what we assumed
Doc 771 landed with a wrong assumption baked in. The DEEP cross-ref caught it. The correction PR (#736) is the system working correctly — autonomous research generates claims, verification falsifies them, the doc gets updated. This is the research flywheel working as designed.

The FCG kernel remains proprietary. The only hook into Bonfires' graph layer is through the `getzep/graphiti` fork. That's the substrate to study.

### CRM Direction Reset
Doc 772 formalizes what the May 29 call decided: Airtable is out, Supabase-native is in. The CRM lives in ZAOOS, gated by RLS, fed by ZOE over a bot API. Public `/network` feed + private `/crm` dashboard. No new infrastructure — just the right use of what already exists. Migration SQL is a proposal; Zaal signs off before any schema runs.

---

## Items Tagged for Tomorrow (Sunday May 31)

- [ ] **MERGE** PR #733 — ZOE orchestrator HIGH fixes (82 tests pass, tsc clean). After merge: VPS redeploy + smoke test `plan: test routing`.
- [ ] **MERGE** PR #736 — Doc 771 trimtab correction (clean correction, ready now)
- [ ] **MERGE** PR #734 — Doc 772 CRM research (docs only, no schema runs until Zaal signs off)
- [ ] **DECIDE** — CRM migration SQL from doc 772 (Zaal sign-off required before schema runs)
- [ ] **FIX** — AgentMail allowlist (`.claude/settings.json`) — Day 40. 5 min. Do it.
- [ ] **TASK** — Post ZABAL Games + ZAOstock combined announce. Day 4+ in queue. Ship it.
- [ ] **TASK** — Build ZABAL Games entry on Magnetic (Tyler is waiting)

---

## Notes

- Today was a two-track Saturday: ZOE safety (audit → fix) and Bonfires research (ship → correct)
- QKMS verdict is closed: noble is THE signer (doc 762 resolved, typecheck green)
- ZAO Farcaster multi-agent (doc 761) Phases 1–4 are in main; FFX exec and Klearu stubs are present
- No inbox items processed today (AGENTMAIL_API_KEY unavailable in container)
