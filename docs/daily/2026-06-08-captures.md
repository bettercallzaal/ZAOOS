# Captures — Monday June 8, 2026

*ZOE nightly processing — inbox blocked (AgentMail Day 47 allowlist issue); captures drawn from today's git activity + PR state.*

---

## Git Activity (today, June 8)

| Commit | What |
|--------|------|
| `eedbb8f` | Merge PR #805 — cowork control plane audit lands on main |
| `d9f4748` | Docs: agents audit doc 816 — cowork control plane + todo/GitHub state (tier:STANDARD) |

**Today's PRs merged:** 1 (PR #805)
**Open PR:** #806 (Spaces: backlog + leaderboard + production hardening)

---

## Doc 816 — Cowork Control Plane Audit

Audited the full Phase 1-4 control plane implementation for the cowork bot. Covers the observe client (Phase 1 — `reportEvent` + heartbeat metaFn), control client (Phases 2-4 — full bidirectional task/converse/control loop), the fleet board additions (Magnetiq, AttaBotty, Hermes), and the todo/GitHub state tracker that keeps the phase pipeline honest.

This audit closes the loop on the sprint: the cowork control plane design (Doc 800) → implementation (Phases 1-4) → audit (Doc 816). Phase 3 fold-in of ZAO Devz to Hermes still pending.

**Category:** agents / cowork / reference

---

## PR #806 — Spaces: Backlog + Leaderboard + Production Hardening (OPEN)

The Spaces branch is ready for production but waiting on owner-action prerequisites:

**What shipped in the branch:**
- Ghost-room cleanup: 0-peer confirmed sweep after 15m grace (6h TTL for never-joined rooms). Folded into existing `/api/cron/juke-stale-rooms` — Vercel caps at 4 crons, so Juke + 100ms share one schedule.
- Real participant counts: live 100ms peer count per room, fault-tolerant.
- `room_id_100ms` persisted on first join.
- Demote-from-stage: host panel + Remove button.
- Pinned links / agenda: host-only PATCH, max 10, inline editor.
- Dead-code retired: `HMSRoom` + `HMSRoomAdapter` deleted.
- 100ms rooms feed leaderboard: `space_sessions` recorded on entry/close (parity with Stream).
- Leaderboard tab on `/spaces`: week/month/all-time, FID-enriched names/avatars.
- 100ms webhook: peer join/leave, session close, recording success. Guarded by `HMS_WEBHOOK_SECRET`.
- RLS hardening SQL: drops permissive anon write policies. **Run once in prod.**

**Build status:** `npm run typecheck` → 0 errors. 37 API tests passing. CI green.

**Production prerequisites (Zaal action needed before merge):**
1. Add `NEXT_PUBLIC_100MS_ACCESS_KEY`, `HMS_APP_SECRET`, `NEXT_PUBLIC_100MS_TEMPLATE_ID`, `CRON_SECRET`, `HMS_WEBHOOK_SECRET` to Vercel
2. Confirm `ms_rooms` + `speaker_requests` + `space_sessions` schema exists in prod
3. Run `scripts/spaces-rls-hardening.sql`
4. Configure 100ms dashboard webhook → `https://<domain>/api/100ms/webhook`

**Category:** feature / spaces / production-ready

---

## Spaces Sprint Summary (Phases shipped this sprint — for the record)

| PR | What |
|----|------|
| #796 | Songjam room type + isolated multiplayer test room |
| #797 | Audit test room v2: camera, screen share, video grid |
| #798 | 100ms upgrade to working A/V room (video + screen share) |
| #799 | Camera-off default, guest fallback, /spaces listing |
| #800 | Token gates enforced on 100ms rooms |
| #801 | Stage mode: raise hand → host approves → speak |
| #802 | (sync) |
| #803 | Spaces polish: conventional controls, presenter view, spotlight, reactions |
| #804 | Fleet board: Magnetiq, AttaBotty, Hermes |
| **#806** | **Backlog + leaderboard + production hardening (OPEN)** |

---

## Calendar Events — Today

- **Fractal meeting, 6pm EST** — ZAO contribution vote. Zaal's contribution update: Docs 803-816, ZABAL Games fireside (Carlos), Colorado A-Corp.

---

## Tasks Identified

- [ ] Merge PR #806 — after running production prerequisites above
- [ ] Complete the 4 production prerequisite steps for 100ms Spaces (env vars, schema, RLS script, webhook config)
- [ ] AgentMail allowlist fix — **Day 47** — add `api.agentmail.to` to `.claude/settings.json`

---

*Inbox: 0 items processed (api.agentmail.to blocked — Day 47 allowlist fix outstanding)*
