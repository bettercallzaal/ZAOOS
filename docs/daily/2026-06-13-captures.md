# Captures — Friday June 13, 2026

*ZOE Nightly Processing | Run at 10pm EST*

---

## Inbox (AgentMail)

**Status: SKIPPED — API key unavailable in cloud environment.**

`AGENTMAIL_API_KEY` is not present in the ephemeral container environment. Day 50 of this blocker.
Fix: add `AGENTMAIL_API_KEY=...` to `~/.zao/zao.env` on VPS, or run the nightly processor from a local session.

Inbox items: **0 processed**.

---

## Today's Shipped Research (Git Activity)

### PR #848 — ZAO Estate Control Plane (major ship)

**What:** Automated truth-keeping for the entire ZAO stack. One `run-checks` CLI produces `estate-report.json` and fans out to three surfaces: PR guardrail (sticky comment + ratchet), self-contained dark dashboard, and Telegram digest.

**Engine checks (`tools/estate-control-plane/`):**
- **drift:** documented component counts (CLAUDE.md/AGENTS.md) vs live repo + stale docs
- **zombie:** decommissioned/graduated code still present + half-built markers
- **quality:** untested API domains, npm audit (baselined), typecheck (heavy mode)
- **estate:** token-gated; reports manual-scan freshness
- Isolated checks via `Promise.all`, health score, JSON report

**Adoption safety:** Ratchet (existing debt allowed, new debt blocks PR), npm audit baseline allowlist, change-only digests. Repo-agnostic via `config.json`.

**CI:** `.github/workflows/estate-health.yml` — PR guardrail on every PR + weekly sweep with dashboard/digest artifacts.

**Tests:** 8 passing against a programmatic fixture repo. Verified against live repo — correctly caught all 6 real count drifts + the phantom `contracts/` path.

**Design spec:** `docs/superpowers/specs/2026-06-12-estate-control-plane-design.md`

---

## Week Context (Friday = W24 close)

This week (June 7–13) closed with 26+ PRs merged and 158 commits. Full recap → `docs/weekly/2026-W24-recap.md`.

---

## Action Items Surfaced

- **Bonfire labeling run** — **DEADLINE: TOMORROW (June 14)**. One admin action. Do not miss.
- **ZABAL Games open-calls decisions** — Tyler still waiting for Magnetic entry.
- **Week 24 Farcaster thread** — Draft in `docs/weekly/2026-W24-recap.md`. Ready to post.
- **AgentMail allowlist fix** — Day 50. 5 minutes. Add `api.agentmail.to` to `.claude/settings.json`.

---

## Notes

- Estate Control Plane is repo-agnostic — can be ported to Fishbowlz or any future graduate.
- ZAOstock paused June — full focus ZABAL Games + workshops.
- Bonfires refactor deadline June 14 — do NOT wire 2nd agent until after.
