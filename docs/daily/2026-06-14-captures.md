# Captures — Sunday June 14, 2026

*ZOE Nightly Processing | Run at 10pm EST*

---

## Inbox (AgentMail)

**Status: SKIPPED — network egress policy blocks `api.agentmail.to`.**

Day 51. The fix is in the June 13 task list: add `api.agentmail.to` to network allowlist in `.claude/settings.json`. This is a 5-minute fix that has now blocked 51 consecutive nightly inbox runs.

Inbox items: **0 processed**.

---

## Today's Shipped Research (Git Activity)

### PR #851 — Doc 851: ZAO Agent Capabilities Playbook (tier: STANDARD)
- **What:** Institutional memory capture: everything an AI coding agent can now *do* in the ZAOOS estate, and the friction traps that already cost time once. Covers estate census tooling, adversarial audit workflow, Estate Control Plane, CLAUDE.md alignment standard, and hard-won lessons from this month's build sprint.
- **Key capabilities documented:** estate census (audit.sh → 86 Vercel projects, 54 dead), adversarial multi-agent audit (N finders → independent refute-by-default verifiers, ~2-3M tokens/run), Estate Control Plane drift/zombie/quality checks, two-layer CLAUDE.md alignment model.
- **Key friction traps captured:** `npm --prefer-offline` corruption (phantom typecheck errors), `gh` GraphQL rate limit workaround (REST fallback), Actions secrets not available in `if:` context, `vi.clearAllMocks()` mock queue leaks, Workflow result-shaping gotchas.
- **Why it matters:** Future agents surface capabilities they'd otherwise forget, avoiding re-discovery of solved problems. The find-then-refute audit pattern refuted 20 false positives vs single-agent sweeps.

### PR #852 — Doc 852: @thezao.com Email Addresses on Workspace (tier: STANDARD)
- **What:** Practical guide for giving every ZAO brand, project, and role its own @thezao.com address — free, on the Workspace seat already paid for.
- **Key finding:** Up to 30 aliases per user + unlimited Google Groups = no additional cost. Role addresses (submissions@, volunteer@, hello@, zaofestivals@, press@, bookings@) → aliases that land in one inbox. Team addresses → Groups (multiple recipients, survives handoffs).
- **Key call-out:** Cloudflare Email Routing CANNOT coexist with Workspace on the same domain (MX conflict). Workspace-native is the right path.
- **Action ready:** Add aliases in Admin console → Directory → Users → "Add alternate emails." Enable "Send mail as" in Gmail for brand-consistent replies. Set catch-all in Admin routing.
- **Related:** If A-Corp/entity status lands (doc 804), apply to Google for Nonprofits → free Workspace seat.

---

## Action Items Surfaced

- **Bonfire labeling run** — June 14 deadline is **TODAY** (this is the last window). Everything downstream blocked (135-doc backfill, cross-bot KG, read vectors). Did you do it?
- **Next.js P0 CVE** — `npm install next@16.2.9`. CVSS 7.5 DoS. Still sitting in production.
- **Add @thezao.com aliases** — `submissions@`, `volunteer@`, `hello@`, `zaofestivals@`, `press@` via Admin console. Zero cost. Ready to go (doc 852).
- **ZABAL Games open-calls decisions** — Tyler still waiting. 5 decisions needed.
- **AgentMail network allowlist** — Day 51. 5 minutes to fix.

---

## Notes

- Docs through: **852**
- Open PRs at end of day: **0**
- Today: Sunday (Bonfire deadline day)
