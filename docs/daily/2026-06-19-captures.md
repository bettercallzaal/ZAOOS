# Captures — Friday June 19, 2026

*ZOE Nightly Processor | Generated 10pm EST*

---

## Inbox

AgentMail blocked — Day 59 of the 5-minute fix. `api.agentmail.to` not in `.claude/settings.json` allowlist.
Inbox drained in worksession Thursday (7 items → docs 875–878). No new unread messages expected.
**Fix is 5 minutes. Add the domain. Stop leaving this.**

---

## Today's Git Activity (4 PRs merged to main)

The scrape subsystem loop completed. Four final PRs landed, closing out the ~14-iteration autonomous build.

### PRs Merged Today

**PR #901 — BetterCallZaal site scraper**
`feat(scrape): bettercallzaal.com site scraper (+ ?bczSite= on /api/scrape)`
Full no-login scrape of bettercallzaal.com. Wired into the unified `/api/scrape` dispatcher.

**PR #900 — X user-timeline scraper**
`feat(scrape): X user-timeline scraper (no login) + ?xUser= on /api/scrape`
Paginated user timeline via FxTwitter API — no X account, no cookie. Wired to dispatcher.

**PR #898 — Farcaster username resolver**
`feat(scrape): resolve Farcaster username to fid (+ ?farcasterUser= on /api/scrape)`
Resolves @handle → FID via Neynar API. Dispatcher can now accept usernames, not just raw FIDs.

**PR #899 — Respect-sync silent-zero fix**
`fix(respect-sync): do not write 0 balance on failed on-chain read`
Stops a governance correctness bug: failed RPC reads were silently writing 0 RESPECT balance to the DB, overwriting real values. Now skips the write on failure. Zero-silent-failure pattern closed.

---

## Open PRs After Tonight (3 remaining)

| PR | Title | Description |
|----|-------|-------------|
| **#902** | WaveWarZ spotlight tier single-source fix | Derives tier order from SPOTLIGHT_TIERS directly (was a parallel hardcoded array). 7 tests. |
| **#903** | BCZ unified profile aggregator | Composes site + X timeline + Farcaster history via Promise.allSettled. Fault-tolerant. 5 tests. |
| **#904** | Doc 880: scrape subsystem institutional memory | Research doc capturing the full loop: what shipped, how it works, process learnings. STANDARD tier. |

All 3 are off clean main. Ready to merge.

---

## Week 25 Snapshot

| Metric | Count |
|--------|-------|
| Commits to main this week | ~97 |
| PRs merged | 20+ |
| Research docs created | 6 (docs 873–878 + 880 in queue) |
| New scrapers shipped | 7 (X Article, WaveWarZ battles, BCZ site, X timeline, Farcaster resolver, unified dispatcher, BCZ profile aggregator) |
| New features | 3 (Unlock NFT ticketing, live bot fleet status, scrape cache persistence) |
| ZOE upgrades | 12+ (proactive Bonfire relay, CI detection, stale PR watcher, multi-agent fan-out) |
| Governance fixes | 2 (respect-sync silent-0, WaveWarZ spotlight single-source) |

---

## Overdue Items

- **Bonfire labeling run** — **6 days overdue** (deadline June 14). Still blocking ALL Bonfire downstream work.
- **Next.js CVE** — 6 days in production. CVSS 7.5 DoS. P0. Still unpatched.
- **Arun calendar invite** — Workshop is TOMORROW (Sat June 20). Unknown if sent. Confirm.
- **Week 23 + Week 24 Farcaster threads** — Both drafted, both not posted.

---

## Notes

- Today ends Week 25. The scrape subsystem is functionally complete: 7 scrapers + unified dispatcher + retry + cache + profile aggregator. Doc 880 captures the institutional memory.
- Weekly recap due today at 4:30pm — see `docs/weekly/2026-W25-recap.md`
- Saturday June 20 is Arun's AI music marketing workshop
- June 26 (7 days) — Bayo's traditional ceremony, Robert Treat Hotel, Newark NJ, 4pm
