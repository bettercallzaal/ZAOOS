# Captures — Sunday September 6, 2026

*ZOE Nightly Processing — 10pm EST*

---

## Inbox

**AgentMail unavailable — Day 121.**

`AGENTMAIL_API_KEY` is still missing from `~/.zao/zao.env`. No messages processed.

Resolution path: `echo "AGENTMAIL_API_KEY=<key>" >> ~/.zao/zao.env` — key is in 1Password under "ZOE AgentMail".

---

## Today's Activity Summary

**3 code fixes merged today:**
- `#3408` — fix(zoe): agent-markup strip was covering the wrong bot; ZOE could still ship raw `<think>` blocks
- `#3410` — fix(devz): ZAO Devz bots never got the agent-markup strip that ZOE received last week
- `#3412` — fix(zoe): send budget was turning a capped relay into permanent data loss (a cap that drops messages silently vs. a cap that stops and waits)

**#3400 also merged** (fix(bot): agent-markup guard on every bot) — was in the ready queue.

**0 new captures** (inbox blocked).

---

## Open PRs Going Into Monday

| PR | Title | Age |
|---|---|---|
| #3413 | fix(zoe): send budget still drops board mentions + teammate acks | 1 day |
| #3407 | fix(settings): drop 2 Write() deny rules harness was skipping | 2 days |

---

*Next capture run: Monday September 7, 2026 — 10pm EST*
