# Captures — Monday September 7, 2026

*Processed by ZOE nightly at 10pm EST*

---

## Inbox Status

**AgentMail: UNAVAILABLE — Day 121**

`AGENTMAIL_API_KEY` is still missing from `~/.zao/zao.env`. Key is in 1Password under "ZOE AgentMail". This has been the blocker since Day 1.

No inbox items processed tonight. All other steps completed normally.

---

## Today's Activity (from git log)

**13 commits merged — ZOE send-layer sweep:**

### ZOE Fixes (7)
- `#3427` fix(zoe): PARTIALLY sent inbound relay marked pushed — tg_pushed was the only dedup gate
- `#3429` fix(zoe): ZAOstock approvals surfacer advanced cursor past messages that never arrived
- `#3426` fix(zoe): Klearu safety gate failed OPEN when classifier ran and said nothing
- `#3424` fix(zoe): PARTIALLY delivered morning batch still lost the deferred queue
- `#3423` fix(zoe): morning batch cleared deferred queue before the send, so one swallowed error lost everything
- `#3419` fix(zoe): daily post draft card was capped away but recorded as delivered
- `#3418` fix(zoe): send budget made pinned brief report 'pinned 0' for messages nobody got
- `#3416` fix(zoe): router threw away the send verdict, so capped nudges were recorded as delivered

### Settings / Infrastructure
- `#3407` fix(settings): drop 2 Write() deny rules the harness was skipping *(merged)*

### Research / Docs
- `#3428` doc 2470: avoiding fabrication in agentic systems (STANDARD)
- `#3425` doc 2469: loops vs graphs, and the 100-line agent that proves the point (STANDARD)
- `#3430` radar: revalidate doc 687 (claude-code-workflow-context-patterns)
- `#3422` NYC weekend record + three ZAO NYC plan corrections + venue-address allowlist for PII scanner

**Theme of the day:** Every ZOE fix was the same shape — messages marked delivered when they weren't. The send layer was systematically over-reporting success across 7 separate code paths. All cleared in one sweep.

---

## Open PRs at Close of Day

**0** — all cleared.

---

*Next doc number: 2471*
