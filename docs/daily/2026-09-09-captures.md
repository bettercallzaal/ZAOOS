# Captures — Wednesday September 9, 2026

*H2 Day 72 | ZAOstock T-26*

---

## Inbox Status

**AgentMail:** Unavailable — Day 123. `AGENTMAIL_API_KEY` missing from `~/.zao/zao.env`.
Key is in 1Password under "ZOE AgentMail". One line to unlock: `echo "AGENTMAIL_API_KEY=<key>" >> ~/.zao/zao.env`

*No inbox items processed tonight. All categories below reflect 0 new captures from AgentMail.*

---

## Processed Items

*(None — inbox blocked)*

---

## Day Summary from Git

10 commits landed today (excluding last night's nightly):

| Commit | Description | Type |
|---|---|---|
| `587cd4f` | docs(2475): four "runs nowhere" instances fixed, rec 6 wrong attribution | research/fix |
| `73a9a43` | doc 2477: business research (tier:STANDARD) | research |
| `002419e` | test: pin Date.now in livepeer clip unique-name test | fix |
| `15cb5fb` | retract: Lil WaveWarZ Takeover never happened, removed from 6 files | retraction |
| `d959c7a` | wall: `--to-zaal` destination + every write reports landing location | feature |
| `0f22014` | wall: irreversible/decision flag routes card to Zaal regardless of lane | feature |
| `9f3b6a1` | doc 2478: inverted alarms — signals that go quiet (DEEP tier) | research |
| `3e7b75c` | research: 673 records the doc that superseded it | correction |
| `d6088dc` | research: correct 2471 — `~/bin` is a symlink, two decisions void | correction |
| `366079e` | research: land 2375 supersession fix + two source corrections | correction |

**Next doc number: 2479**

---

## Notable: Doc 2478 — Inverted Alarms (DEEP)

A DEEP-tier research doc on the failure class where a system goes *quiet* instead of firing.
The complement to `noisy-signal-guard.md` (false alarms) — this covers signals that were
working, then stopped, and nobody noticed because silence looks like "nothing happening."
Filed alongside `silent-failure-guard.md` in the rules family. High-value institutional memory.

## Notable: Retraction (#3460)

The Lil WaveWarZ Takeover was listed as a shipped event in 6 files across the repo. [CLAIM-OK: takeover-2025 the nightly capture naming the retracted claim in order to record its retraction; "It never happened" is the next line, and exemptions are line-scoped by design]
It never happened. The retraction PR (#3460) removes all 6 instances.
This is the right move — a false event in docs is worse than a gap.

## Notable: Wall Routing Upgrades (#3452, #3456)

The zao-wall now has:
- `--to-zaal` as an explicit human destination (irreversible/decision cards bypass lane routing)
- Every write call reports where the card actually landed (not just whether it was written)
These two together close the "card written but went nowhere" failure mode that `docs(2475)` documented.
