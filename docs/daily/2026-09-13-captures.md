# Captures — Sunday September 13, 2026

*ZOE Nightly Processing*

---

## Inbox Status

**AgentMail: BLOCKED — Day 126**

The `AGENTMAIL_API_KEY` environment variable is not set in `.env.local`. ZOE cannot access `zoe-zao@agentmail.to`. Key is in 1Password "ZOE AgentMail".

Fix: `echo "AGENTMAIL_API_KEY=<key>" >> ~/.zao/zao.env`

No inbox items processed tonight.

---

## Today's Activity (from git log)

19 commits landed Sunday September 13. Three themes:

### 1. Merge-Commit Hardening Suite (14 PRs)

Every pre-commit gate — doc-guard, pii-scan, secret-scan, research-index — was blind to what a merge commit brings in from the other parent. Today's suite closed that blindspot systematically:

- **doc-guard** (×5): merge creates duplicate numbers neither side authored; date-named files held doc numbers nobody claimed; renumbered-onto-taken-number bypass; regression from #3494; base fix
- **pii-scan** (×2): merge commit is not the author of the other parent's lines; empty MERGE_HEAD diff is a valid answer not a failure
- **secret-scan** (×2): same merge-commit authorship fix; renamed-and-edited file bypassed the gate entirely (#3444)
- **research-index**: stopped reporting the other parent's docs; stopped saying BLOCKED
- **RLS**: verification query was erroring and rolling back the whole hardening pass
- **docs-automerge**: the one gate that merges with nobody present was blind to renames
- **lanes-board**: phone lost critical lanes when criticality stopped being a state
- **CI**: estate-control-plane tests have never been executed — now they run

### 2. Loop-Evals Rule Update (doc: .claude/rules/)

Added "review the direction the change was FOR, not only the direction you fear" section. This landed from ZAOOS #3502/#3505 — a security gate fix that shipped still broken in exactly the case the PR was written to close because the reviewer verified the dangerous direction (under-reporting) but never verified the direction the PR existed to fix (blocking what it should block). Cross-referenced with `~/zao-vault/AGENTS.md` rule 10.

### 3. ZOE Reconcile Cadence

ZOE now reconciles every 10 minutes (#3459). Closed cards leave the count the same hour instead of accumulating as ghost entries.

---

## Items for Tomorrow

- **Fractal meeting 6pm EST** — Monday
- **Deal memos to 5 verbal confirms** — Day 10 overdue by tomorrow morning
- **Sponsor email** — Day 19 overdue
- **VPS deploy** — commits accumulating; ZOE send-layer smoke test still pending
